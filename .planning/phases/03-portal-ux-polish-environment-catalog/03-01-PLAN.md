---
phase: 03-portal-ux-polish-environment-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/portal/src/components/layout/Shell.vue
  - apps/portal/src/router/index.ts
  - apps/portal/src/views/Projects.vue
  - apps/portal/src/views/ProjectDetail.vue
  - apps/portal/src/views/settings/SettingsEnvironments.vue
  - apps/portal/src/views/settings/SettingsPreferences.vue
  - apps/portal/src/views/settings/SettingsPlatform.vue
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "Sidebar shows 'APIs Catalog' instead of 'Projects'"
    - "Projects.vue h1 reads 'APIs' and eyebrow reads 'APIs Catalog'"
    - "ProjectDetail breadcrumb link text reads 'APIs Catalog'"
    - "Settings nav item expands into Environments, Preferences, Platform sub-items"
    - "Navigating to /settings redirects to /settings/environments"
    - "Each settings sub-route renders its own placeholder page"
  artifacts:
    - path: "apps/portal/src/components/layout/Shell.vue"
      provides: "Settings collapsible group with 3 sub-items"
      contains: "APIs Catalog"
    - path: "apps/portal/src/router/index.ts"
      provides: "Settings child routes with redirect"
      contains: "/settings/environments"
    - path: "apps/portal/src/views/settings/SettingsEnvironments.vue"
      provides: "Environments placeholder page"
    - path: "apps/portal/src/views/settings/SettingsPreferences.vue"
      provides: "Preferences placeholder page"
    - path: "apps/portal/src/views/settings/SettingsPlatform.vue"
      provides: "Platform placeholder page"
  key_links:
    - from: "Shell.vue"
      to: "/settings/*"
      via: "router-link sub-items"
      pattern: "router-link.*to.*settings"
    - from: "router/index.ts"
      to: "settings views"
      via: "route children with redirect"
      pattern: "redirect.*settings/environments"
---

<objective>
Rename navigation labels throughout the portal ("Projects" to "APIs Catalog") and refactor the Settings nav item into a collapsible submenu with three sub-routes: Environments, Preferences, and Platform.

Purpose: Aligns navigation terminology with domain language and establishes the settings page structure needed for the Environment Catalog feature.
Output: Updated Shell.vue with collapsible settings group, updated label text in Projects.vue and ProjectDetail.vue, new router child routes, and three settings placeholder views.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-portal-ux-polish-environment-catalog/03-UI-SPEC.md

<interfaces>
<!-- Existing nav structure in Shell.vue (lines 148-154) -->
const mainNav = [
  { path: '/dashboard',    icon: 'explore',     label: 'Explorer'     },
  { path: '/projects',     icon: 'inventory_2', label: 'Projects'     },
  { path: '/analytics',    icon: 'analytics',   label: 'Analytics'    },
  { path: '/integrations', icon: 'hub',         label: 'Integrations' },
  { path: '/settings',     icon: 'settings',    label: 'Settings'     },
];

<!-- Current router routes (router/index.ts) — no /settings routes exist -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename nav labels and update Shell.vue with Settings collapsible group</name>
  <files>
    apps/portal/src/components/layout/Shell.vue,
    apps/portal/src/views/Projects.vue,
    apps/portal/src/views/ProjectDetail.vue
  </files>
  <read_first>
    apps/portal/src/components/layout/Shell.vue,
    apps/portal/src/views/Projects.vue,
    apps/portal/src/views/ProjectDetail.vue,
    .planning/phases/03-portal-ux-polish-environment-catalog/03-UI-SPEC.md
  </read_first>
  <action>
**Label renames (per UI-SPEC Section 2):**

1. In `Shell.vue`, change the `mainNav` array entry for `/projects`: `label: 'Projects'` becomes `label: 'APIs Catalog'`.
2. Remove the `/settings` entry from `mainNav` entirely — Settings will be rendered as a separate collapsible group below the `mainNav` loop.
3. In `Projects.vue`, change the `<h1>` text from `Projects` to `APIs`. Change the eyebrow `<span>` text from `Workspace` to `APIs Catalog`.
4. In `ProjectDetail.vue`, change the breadcrumb `<router-link>` text from `Projects` to `APIs Catalog` (line 33 area).

**Settings collapsible group (per UI-SPEC Section 1):**

5. In `Shell.vue` `<script setup>`, add:
   - `const settingsOpen = ref(...)` — initialized in a `watchEffect` or computed: `settingsOpen` defaults to `true` when `route.path.startsWith('/settings')`, otherwise `false`. Use `ref` with an `onMounted` or `watchEffect` that sets it based on the current route.
   - `const settingsNav` array:
     ```
     const settingsNav = [
       { path: '/settings/environments', icon: 'dns',             label: 'Environments' },
       { path: '/settings/preferences',  icon: 'manage_accounts', label: 'Preferences'  },
       { path: '/settings/platform',     icon: 'tune',            label: 'Platform'     },
     ];
     ```
   - Add `import { ref, computed, watchEffect } from 'vue';` (ensure `ref` and `watchEffect` are imported).

6. In the sidebar template, after the `mainNav` loop `</nav></div>` block (the "Developer Hub" section), add a new section for Settings:

   ```html
   <!-- Settings section -->
   <div>
     <button
       @click="settingsOpen = !settingsOpen"
       class="flex items-center gap-2.5 px-3 py-2 rounded-xl text-sm font-medium transition-all duration-150 w-full"
       :class="route.path.startsWith('/settings')
         ? 'bg-white shadow-sm'
         : 'text-slate-500 hover:bg-white/70 hover:text-slate-700'"
       :style="route.path.startsWith('/settings') ? 'color: #0058bc;' : ''"
     >
       <span class="material-symbols-outlined" style="font-size: 19px;">settings</span>
       <span class="flex-1 text-left">Settings</span>
       <span class="material-symbols-outlined" style="font-size: 18px;">
         {{ settingsOpen ? 'expand_less' : 'expand_more' }}
       </span>
     </button>
     <div
       v-show="settingsOpen"
       class="overflow-hidden transition-all duration-200 ease-in-out"
     >
       <nav class="mt-0.5 space-y-0.5">
         <router-link
           v-for="sub in settingsNav"
           :key="sub.path"
           :to="sub.path"
           class="flex items-center gap-2 pl-[28px] pr-3 py-1.5 rounded-xl text-xs font-medium transition-all duration-150"
           :class="isActive(sub.path)
             ? 'bg-white shadow-sm'
             : 'hover:bg-white/70'"
           :style="isActive(sub.path)
             ? 'color: #0058bc; box-shadow: 0 1px 4px rgba(0,0,0,0.08);'
             : 'color: #414755;'"
         >
           <span
             class="material-symbols-outlined"
             :style="`font-size: 17px; font-variation-settings: 'FILL' ${isActive(sub.path) ? 1 : 0};`"
           >{{ sub.icon }}</span>
           {{ sub.label }}
         </router-link>
       </nav>
     </div>
   </div>
   ```

   The sub-item text uses `text-xs font-medium` (weight 500 per UI-SPEC). Left indent is `pl-[28px]`. Active state has white background, accent color `#0058bc`, and box-shadow `0 1px 4px rgba(0,0,0,0.08)`. Icon uses `FILL 1` when active.
  </action>
  <verify>
    <automated>cd apps/portal && npx vue-tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Shell.vue contains the string `'APIs Catalog'` as a mainNav label
    - Shell.vue does NOT contain `{ path: '/settings',` in the mainNav array
    - Shell.vue contains `settingsNav` array with paths `/settings/environments`, `/settings/preferences`, `/settings/platform`
    - Shell.vue contains `settingsOpen` ref
    - Shell.vue contains `pl-[28px]` for sub-item indent
    - Shell.vue contains `expand_less` and `expand_more` for chevron toggle
    - Projects.vue `<h1>` contains text `APIs` (not `Projects`)
    - Projects.vue eyebrow contains `APIs Catalog` (not `Workspace`)
    - ProjectDetail.vue breadcrumb router-link text contains `APIs Catalog` (not `Projects`)
  </acceptance_criteria>
  <done>All four label renames applied. Settings collapsible group renders with 3 sub-items, auto-expands on /settings/* routes, and collapses with chevron toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Add Settings child routes and placeholder views</name>
  <files>
    apps/portal/src/router/index.ts,
    apps/portal/src/views/settings/SettingsEnvironments.vue,
    apps/portal/src/views/settings/SettingsPreferences.vue,
    apps/portal/src/views/settings/SettingsPlatform.vue
  </files>
  <read_first>
    apps/portal/src/router/index.ts,
    apps/portal/src/components/layout/Shell.vue,
    .planning/phases/03-portal-ux-polish-environment-catalog/03-UI-SPEC.md
  </read_first>
  <action>
**Create three placeholder settings views** in `apps/portal/src/views/settings/`:

Each view follows the same pattern as Projects.vue layout: wrapped in `<Shell>`, using `p-8 max-w-7xl mx-auto`, with an eyebrow label "Settings" (`text-xs font-bold uppercase tracking-widest`, color `#0058bc`).

1. **SettingsEnvironments.vue:**
   ```vue
   <template>
     <Shell>
       <div class="p-8 max-w-7xl mx-auto" style="font-family: 'Inter', sans-serif;">
         <span class="text-xs font-bold tracking-widest uppercase mb-2 block" style="color: #0058bc;">Settings</span>
         <h1 class="text-5xl font-bold tracking-tight" style="color: #1a1b1f;">Environments</h1>
         <p class="mt-2 text-sm" style="color: #414755;">Manage deployment environments and their tags.</p>
       </div>
     </Shell>
   </template>
   <script setup lang="ts">
   import Shell from '../../components/layout/Shell.vue';
   </script>
   ```

2. **SettingsPreferences.vue:** Same structure, `<h1>` text "Preferences", subtitle "User preferences and notification settings."

3. **SettingsPlatform.vue:** Same structure, `<h1>` text "Platform", subtitle "Platform-wide configuration and administration."

**Update router/index.ts:**

4. Add imports at the top:
   ```ts
   import SettingsEnvironments from '../views/settings/SettingsEnvironments.vue';
   import SettingsPreferences from '../views/settings/SettingsPreferences.vue';
   import SettingsPlatform from '../views/settings/SettingsPlatform.vue';
   ```

5. Add settings routes to the `routes` array (after the `/integrations` placeholder route, before the array closes):
   ```ts
   {
     path: '/settings',
     redirect: '/settings/environments',
     meta: { requiresAuth: true },
     children: [
       { path: 'environments', component: SettingsEnvironments },
       { path: 'preferences', component: SettingsPreferences },
       { path: 'platform', component: SettingsPlatform }
     ]
   }
   ```

   Note: Since each settings view wraps itself in `<Shell>`, the parent `/settings` route needs no component — it just redirects. The children use their own `<Shell>` wrapper (matching the pattern used by Projects.vue and ProjectDetail.vue which are top-level routes with `<Shell>`).

   If vue-router requires a component for the parent when children exist, use a pass-through: add `component: { template: '<router-view />' }` to the parent, OR restructure as flat routes:
   ```ts
   { path: '/settings', redirect: '/settings/environments', meta: { requiresAuth: true } },
   { path: '/settings/environments', component: SettingsEnvironments, meta: { requiresAuth: true } },
   { path: '/settings/preferences', component: SettingsPreferences, meta: { requiresAuth: true } },
   { path: '/settings/platform', component: SettingsPlatform, meta: { requiresAuth: true } }
   ```
   Prefer the flat route approach for simplicity — it matches the existing router pattern where each view is a top-level route.
  </action>
  <verify>
    <automated>cd apps/portal && npx vue-tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/portal/src/views/settings/SettingsEnvironments.vue` exists and contains `<h1` with text `Environments`
    - File `apps/portal/src/views/settings/SettingsPreferences.vue` exists and contains `<h1` with text `Preferences`
    - File `apps/portal/src/views/settings/SettingsPlatform.vue` exists and contains `<h1` with text `Platform`
    - All three views contain `import Shell from '../../components/layout/Shell.vue'`
    - All three views contain eyebrow text `Settings` with `color: #0058bc`
    - `router/index.ts` contains route for `/settings` that redirects to `/settings/environments`
    - `router/index.ts` imports `SettingsEnvironments`, `SettingsPreferences`, `SettingsPlatform`
    - `router/index.ts` contains `path: '/settings/environments'` route entry
    - `vue-tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>Three settings placeholder views created. Router has /settings redirect to /settings/environments, plus individual routes for all three settings pages. Type-check passes.</done>
</task>

</tasks>

<verification>
- `npx vue-tsc --noEmit` passes in `apps/portal`
- Shell.vue sidebar shows "APIs Catalog" label for the /projects nav item
- Shell.vue has a Settings collapsible group with 3 sub-items
- Projects.vue h1 reads "APIs", eyebrow reads "APIs Catalog"
- ProjectDetail.vue breadcrumb reads "APIs Catalog"
- /settings route redirects to /settings/environments
- Each settings sub-route loads its placeholder page
</verification>

<success_criteria>
All four navigation label renames applied. Settings collapsible group with Environments/Preferences/Platform sub-items is functional. Three settings routes registered and loading placeholder views. TypeScript compilation passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-portal-ux-polish-environment-catalog/03-01-SUMMARY.md`
</output>
