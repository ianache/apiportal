---
phase: 03-portal-ux-polish-environment-catalog
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/bff/src/routes/apis.ts
  - apps/bff/src/routes/apis.test.ts
  - apps/portal/src/stores/registry.ts
  - apps/portal/src/views/ProjectDetail.vue
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "API metadata changes (name and description) persist after editing"
    - "New API versions are successfully saved and displayed in the catalog"
    - "The version list can be filtered by status (e.g., DESIGN, PUBLISHED)"
    - "New versions are initialized with the 'DESIGN' status by default"
    - "API name and description can be edited directly in the UI with automatic saving"
  artifacts:
    - path: "apps/bff/src/routes/apis.ts"
      provides: "PATCH /apis/:id and POST /apis/:id/versions endpoints"
    - path: "apps/bff/src/routes/apis.test.ts"
      provides: "BFF API tests for metadata and versions"
    - path: "apps/portal/src/stores/registry.ts"
      provides: "updateApi and createVersion Pinia actions"
    - path: "apps/portal/src/views/ProjectDetail.vue"
      provides: "Inline edit inputs, version filter select, and new version creation flow"
  key_links:
    - from: "apps/portal/src/views/ProjectDetail.vue"
      to: "apps/portal/src/stores/registry.ts"
      via: "store actions updateApi and createVersion"
      pattern: "registry\\.(updateApi|createVersion)"
---

<objective>
Wave 2: Versioning & Editing. Implement backend APIs and frontend UI for inline editing of API metadata (name/description), version status filtering/sorting, and new version creation.

Purpose: Empowers API Managers and Designers to manage API metadata and versions directly from the detail view.
Output: Extended BFF api routes with tests, updated Pinia registry store, and interactive ProjectDetail.vue view.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-portal-ux-polish-environment-catalog/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API metadata and version routes with tests to BFF</name>
  <files>
    apps/bff/src/routes/apis.ts,
    apps/bff/src/routes/apis.test.ts
  </files>
  <action>
1. In `apps/bff/src/routes/apis.ts`, add:
   - `PATCH /apis/:id` handler to update `name` and `description` (Prisma update).
   - `POST /apis/:id/versions` handler to create new version with `status: 'DESIGN'`.
   - Enforce RBAC: reject `API_DEVELOPER` role for both endpoints.
2. In `apps/bff/src/routes/apis.test.ts`, create or update tests for these endpoints:
   - Verify happy paths (metadata update, version creation).
   - Verify RBAC rejection for developers.
   - Verify validation for semver version strings if applicable.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/bff -- --run</automated>
  </verify>
  <done>BFF exposes tested endpoints for patching API metadata and creating new versions.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Registry store with new actions</name>
  <files>
    apps/portal/src/stores/registry.ts
  </files>
  <action>
1. Add `updateApi(id: string, payload: { name?: string; description?: string })` action:
   - Call `PATCH /apis/${id}` with token and JSON body.
   - Return the updated API object or throw an error.
2. Add `createVersion(apiId: string, version: string)` action:
   - Call `POST /apis/${apiId}/versions` with `{ version }` body.
   - Return the created version object.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/portal -- --run</automated>
  </verify>
  <done>Pinia registry store has `updateApi` and `createVersion` actions.</done>
</task>

<task type="auto">
  <name>Task 3: Implement inline edit, filter, and version creation UI</name>
  <files>
    apps/portal/src/views/ProjectDetail.vue
  </files>
  <action>
1. **Inline Edit**:
   - Make `name` and `description` editable if `selectedVersion.status !== 'PUBLISHED'` and user is not a developer.
   - Use `v-if="editingName"` to swap the `<h1>` to an `<input>` with auto-focus.
   - Use `@blur` and `@keydown.enter` to trigger save via `registry.updateApi()`.
2. **Version Filter & Sort**:
   - Add `statusFilter` ref and `filteredVersions` computed property.
   - Filter `api.versions` by status (unless 'ALL') and sort descending by `createdAt`.
   - Add a select dropdown for the filter in the template.
3. **New Version Modal**:
   - Implement `handleCreateVersion` that calls `registry.createVersion()`.
   - Add new version to local state and auto-select it.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/portal -- --run</automated>
  </verify>
  <done>ProjectDetail.vue has fully functioning inline editing, version filtering, and version creation UI.</done>
</task>

</tasks>

<verification>
- Start the app, open an API detail view. Click the title to edit it.
- Filter the versions sidebar by status.
- Open the New Version modal, enter a version string, and verify it appears in the sidebar automatically.
</verification>

<success_criteria>
API metadata can be updated in-place and new versions can be created from the UI, with full backend support.
</success_criteria>

<output>
After completion, create `.planning/phases/03-portal-ux-polish-environment-catalog/03-02-SUMMARY.md`
</output>
