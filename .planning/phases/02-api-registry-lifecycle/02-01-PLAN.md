---
phase: 02-api-registry-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/shared-types/index.ts, apps/bff/prisma/schema.prisma, apps/bff/src/routes/apis.ts, apps/bff/src/app.ts, apps/bff/package.json]
autonomous: true
requirements: [API-01, VER-01, LIF-01, RBAC-01]
must_haves:
  truths:
    - "BFF exposes authenticated CRUD endpoints for APIs and Versions"
    - "Status transitions follow the DESIGN -> REVIEW -> APPROVED -> PUBLISHED state machine"
    - "Only users with API-Manager role can approve or publish versions"
  artifacts:
    - path: "apps/bff/prisma/schema.prisma"
      provides: "API and APIVersion models with status enum"
    - path: "apps/bff/src/routes/apis.ts"
      provides: "Registry and Lifecycle API endpoints"
  key_links:
    - from: "apps/bff/src/routes/apis.ts"
      to: "Prisma"
      via: "Database queries"
      pattern: "prisma\\.api\\.(find|create)"
    - from: "apps/bff/src/routes/apis.ts"
      to: "authPlugin"
      via: "request.user role check"
      pattern: "request\\.user\\.role"
---

<objective>
Implement the core API Registry data model and BFF endpoints with lifecycle state management.

Purpose: Provide a robust backend for managing APIs and their versions with enforced status transitions and RBAC.
Output: Updated database schema, shared types, and secured registry endpoints.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-registry-lifecycle/02-RESEARCH.md
@apps/bff/src/plugins/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Test Scaffolding and Auth Registration</name>
  <files>apps/bff/package.json, apps/bff/src/app.ts, apps/bff/vitest.config.ts, apps/bff/src/routes/__tests__/apis.test.ts</files>
  <action>
    - Add `vitest` and `@types/supertest` to `apps/bff` devDependencies.
    - Register `authPlugin` in `apps/bff/src/app.ts` before routes.
    - Create a basic test scaffold to verify endpoint accessibility and auth.
    - Add `test` script to `apps/bff/package.json`.
  </action>
  <verify>
    <automated>cd apps/bff && npm run test</automated>
  </verify>
  <done>BFF has test infrastructure and auth plugin is active.</done>
</task>

<task type="auto">
  <name>Task 1: Model & Contracts Update</name>
  <files>packages/shared-types/index.ts, apps/bff/prisma/schema.prisma</files>
  <action>
    - Update `shared-types` with `API` and `APIVersion` interfaces matching the parent-child model.
    - Expand `APIStatus` enum to include: `DESIGN`, `REVIEW`, `APPROVED`, `PUBLISHED`, `DEPRECATED`, `RETIRED`.
    - Update `prisma/schema.prisma` with `API` and `APIVersion` models and appropriate relations to `User`.
    - Run `npx prisma migrate dev --name add_api_registry`.
  </action>
  <verify>
    <automated>npx prisma validate --schema=apps/bff/prisma/schema.prisma</automated>
  </verify>
  <done>Persistence layer reflects the registry requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registry & Lifecycle Endpoints</name>
  <files>apps/bff/src/routes/apis.ts, apps/bff/src/app.ts</files>
  <behavior>
    - POST /apis: Creates a new API project and an initial '0.1.0' version in 'DESIGN' status.
    - GET /apis: Returns list of APIs with their current versions.
    - POST /apis/:id/versions: Creates a new version (incrementing from previous).
    - POST /apis/:id/versions/:version/status: Transitions status if valid per state machine.
    - RBAC check: Designer can create/edit in DESIGN, but cannot Approve/Publish.
  </behavior>
  <action>
    - Implement `apis.ts` routes using Fastify.
    - Use `semver` for version validation/increment logic.
    - Enforce the transition map: DESIGN -> REVIEW -> APPROVED -> PUBLISHED.
    - Apply role checks using `request.user.role`.
    - Register `apis.ts` in `app.ts`.
  </action>
  <verify>
    <automated>cd apps/bff && npm run test -- routes/apis.test.ts</automated>
  </verify>
  <done>Full Registry CRUD and Lifecycle management operational.</done>
</task>

</tasks>

<verification>
- API Creation results in records in both API and APIVersion tables.
- Unauthorized status transitions are blocked with 403 Forbidden.
- Invalid status transitions are blocked with 400 Bad Request.
</verification>

<success_criteria>
- APIs can be created, listed, and versioned via BFF.
- Lifecycle transitions are enforced via state machine.
- RBAC prevents Designers from performing Manager-level actions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-registry-lifecycle/02-01-SUMMARY.md`
</output>
