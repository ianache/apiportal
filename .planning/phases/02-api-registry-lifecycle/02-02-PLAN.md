---
phase: 02-api-registry-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [apps/portal/src/views/Projects.vue, apps/portal/src/views/ProjectDetail.vue, apps/portal/src/router/index.ts, apps/portal/src/stores/registry.ts, apps/portal/src/components/registry/ApiForm.vue, apps/portal/src/components/registry/VersionList.vue]
autonomous: false
requirements: [UI-02, API-02, VER-02, LIF-02]
must_haves:
  truths:
    - "User can view APIs in a responsive grid or table view"
    - "User can create new API projects through a modal form"
    - "User can view and manage API versions and lifecycle status"
    - "Lifecycle actions (Approve, Publish) are disabled for users without required roles"
  artifacts:
    - path: "apps/portal/src/views/Projects.vue"
      provides: "Main API Registry dashboard"
    - path: "apps/portal/src/stores/registry.ts"
      provides: "Pinia store for API and Version data"
  key_links:
    - from: "apps/portal/src/views/Projects.vue"
      to: "BFF /apis"
      via: "fetch in registry store"
      pattern: "fetch.*apis"
    - from: "apps/portal/src/views/ProjectDetail.vue"
      to: "BFF /apis/:id/versions"
      via: "fetch in registry store"
      pattern: "fetch.*versions"
---

<objective>
Implement the Portal UI for API Registry and Lifecycle management.

Purpose: Provide a user-friendly interface for managing API projects and their evolutionary stages.
Output: A set of views and components for listing, creating, and transitioning APIs and versions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-registry-lifecycle/02-01-SUMMARY.md
@apps/portal/src/components/layout/Shell.vue
</context>

<interfaces>
From packages/shared-types/index.ts (expected after 02-01):
```typescript
export type APIStatus = 'DESIGN' | 'REVIEW' | 'APPROVED' | 'PUBLISHED' | 'DEPRECATED' | 'RETIRED';

export interface API {
  id: string;
  name: string;
  description?: string;
  createdAt: string;
  updatedAt: string;
  versions: APIVersion[];
}

export interface APIVersion {
  id: string;
  apiId: string;
  version: string;
  status: APIStatus;
  createdAt: string;
  updatedAt: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Registry Store and Projects View</name>
  <files>apps/portal/src/stores/registry.ts, apps/portal/src/views/Projects.vue, apps/portal/src/router/index.ts</files>
  <action>
    - Create `registry.ts` Pinia store to fetch and manage APIs.
    - Implement `Projects.vue` with a toggle between "Grid" (card-based) and "Table" (list-based) views.
    - Card view should show: Name, Latest Version, Status Badge, and Date.
    - Update `router/index.ts` to use `Projects.vue` for the `/projects` path.
  </action>
  <verify>
    <automated>npm run build --workspace=apps/portal</automated>
  </verify>
  <done>Projects dashboard is operational and linked in navigation.</done>
</task>

<task type="auto">
  <name>Task 2: API Creation & Detail View</name>
  <files>apps/portal/src/components/registry/ApiForm.vue, apps/portal/src/views/ProjectDetail.vue, apps/portal/src/router/index.ts</files>
  <action>
    - Create `ApiForm.vue` modal component for creating/editing API project metadata (Name, Description).
    - Implement `ProjectDetail.vue` to show detailed information for a specific API.
    - Add route for `/projects/:id` in `router/index.ts`.
  </action>
  <verify>
    <automated>npm run build --workspace=apps/portal</automated>
  </verify>
  <done>User can create new APIs and drill down into details.</done>
</task>

<task type="auto">
  <name>Task 3: Version Management & Lifecycle Actions</name>
  <files>apps/portal/src/components/registry/VersionList.vue, apps/portal/src/views/ProjectDetail.vue</files>
  <action>
    - Create `VersionList.vue` to display and manage versions of an API.
    - Add a "Create New Version" form (SemVer validation).
    - Implement lifecycle transition buttons (Submit for Review, Approve, Publish).
    - Disable Manager-only actions if user does not have `API_MANAGER` role (use `useAuthStore` role detection).
  </action>
  <verify>
    <automated>npm run build --workspace=apps/portal</automated>
  </verify>
  <done>Lifecycle management is fully integrated into the UI with RBAC awareness.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>API Registry Portal UI</what-built>
  <how-to-verify>
    1. Login to Portal as Designer.
    2. Go to 'Projects' and create a new API.
    3. Verify it appears in Grid and Table views.
    4. Click on the project to go to Detail view.
    5. Verify you can 'Submit for Review' but NOT 'Approve'.
    6. Login as Manager and verify you can 'Approve' and 'Publish'.
  </how-to-verify>
  <resume-signal>approved: UI is functional and respects roles</resume-signal>
</task>

</tasks>

<verification>
- Responsive layout works for both Grid and Table views.
- Error handling for failed BFF calls (e.g., duplicate names) is visible to the user.
- Status badges change color based on lifecycle state.
</verification>

<success_criteria>
- Seamless experience for managing the API lifecycle.
- Visual feedback for all state transitions.
- Correct enforcement of RBAC in the UI layer.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-registry-lifecycle/02-02-SUMMARY.md`
</output>
