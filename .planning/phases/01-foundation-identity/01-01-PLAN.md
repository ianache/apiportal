---
phase: 01-foundation-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, apps/portal/package.json, apps/bff/package.json, packages/shared-types/package.json]
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "Monorepo root exists with npm workspaces configured"
    - "Portal app scaffolded with Vue 3, Pinia, and Tailwind CSS"
    - "BFF app scaffolded with Fastify and Prisma initialized"
  artifacts:
    - path: "package.json"
      provides: "npm workspaces configuration"
      contains: '"workspaces": ["apps/*", "packages/*"]'
    - path: "apps/portal/src/main.ts"
      provides: "Vue 3 entry point"
    - path: "apps/bff/package.json"
      provides: "Fastify backend configuration"
  key_links:
    - from: "apps/portal/package.json"
      to: "package.json"
      via: "npm workspace"
    - from: "apps/bff/package.json"
      to: "package.json"
      via: "npm workspace"
---

<objective>
Initialize the Nexus API Manager monorepo and scaffold the core applications: the Portal (Vue 3) and the BFF (Fastify).

Purpose: Establish the project structure and build environments for parallel development.
Output: A functional monorepo with basic Vue and Fastify scaffolds.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-identity/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monorepo Root Initialization</name>
  <files>package.json, .gitignore</files>
  <action>
    - Initialize root `package.json` with `npm init -y`.
    - Configure `workspaces` for `apps/*` and `packages/*`.
    - Create `.gitignore` to ignore `node_modules`, `.env`, and IDE artifacts.
    - Set up root scripts: `dev:portal`, `dev:bff`, `test`, `build`.
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); if(!pkg.workspaces) throw new Error('Workspaces not configured');"</automated>
  </verify>
  <done>Root package.json configured with workspaces and basic scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Portal Application</name>
  <files>apps/portal/package.json, apps/portal/vite.config.ts, apps/portal/src/main.ts</files>
  <action>
    - Scaffold `apps/portal` using Vite (Vue 3, TypeScript).
    - Install core dependencies: `pinia`, `vue-router`, `tailwindcss`, `vitest`.
    - Configure Tailwind CSS (v4 is expected, otherwise v3).
    - Create a basic "Hello Nexus" Vue component in `App.vue`.
  </action>
  <verify>
    <automated>ls apps/portal/src/main.ts && npm run build --workspace=apps/portal</automated>
  </verify>
  <done>Portal scaffolded with Vue 3, Pinia, and Tailwind CSS; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold BFF Application</name>
  <files>apps/bff/package.json, apps/bff/src/app.ts, apps/bff/prisma/schema.prisma</files>
  <action>
    - Initialize `apps/bff` project.
    - Install core dependencies: `fastify`, `@fastify/cors`, `@fastify/env`, `zod`.
    - Install `prisma` as a dev dependency.
    - Initialize Prisma with `npx prisma init`.
    - Create a basic Fastify app in `src/app.ts` with a `/health` endpoint.
  </action>
  <verify>
    <automated>ls apps/bff/src/app.ts && npx prisma validate --schema=apps/bff/prisma/schema.prisma</automated>
  </verify>
  <done>BFF scaffolded with Fastify and Prisma initialized.</done>
</task>

</tasks>

<verification>
- Root `package.json` contains `workspaces`.
- `apps/portal` build completes successfully.
- `apps/bff` Prisma schema is valid.
</verification>

<success_criteria>
- Monorepo structure is established and manageable via root `npm` commands.
- Both apps have their core frameworks and build tools ready.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-01-SUMMARY.md`
</output>
