---
phase: 01-foundation-identity
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [packages/shared-types/index.ts, apps/bff/prisma/schema.prisma, apps/bff/src/routes/health.ts, apps/bff/src/server.ts]
autonomous: true
requirements: [FOUND-02]

must_haves:
  truths:
    - "Shared types package contains initial identity interfaces"
    - "Prisma schema is configured with PostgreSQL and initial models"
    - "BFF health check verifies database connectivity"
  artifacts:
    - path: "packages/shared-types/index.ts"
      provides: "Common TypeScript interfaces"
    - path: "apps/bff/prisma/schema.prisma"
      contains: "model User"
  key_links:
    - from: "apps/bff/src/server.ts"
      to: "PostgreSQL"
      via: "Prisma Client"
      pattern: 'prisma\.\$connect'
---

<objective>
Configure the persistence layer and shared contracts between the Portal and BFF.

Purpose: Define the data structures and establish database connectivity.
Output: Shared type definitions, Prisma models, and a database-aware health check in the BFF.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-identity/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Shared Types Package</name>
  <files>packages/shared-types/package.json, packages/shared-types/index.ts</files>
  <action>
    - Initialize `packages/shared-types` with a `package.json`.
    - Export basic User and Role interfaces (API-Manager, API-Designer, API-Developer).
    - Ensure it's correctly linked in the monorepo root.
  </action>
  <verify>
    <automated>node -e "require('./packages/shared-types/package.json')"</automated>
  </verify>
  <done>Shared types package created with core identity definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Prisma and Database Schema</name>
  <files>apps/bff/prisma/schema.prisma, apps/bff/.env</files>
  <action>
    - Configure PostgreSQL as the datasource in `prisma/schema.prisma`.
    - Define initial models: `User` (sub, email, role), `AuditLog` (action, userId, timestamp).
    - Create a `.env` template for the BFF with a `DATABASE_URL` placeholder.
  </action>
  <verify>
    <automated>npx prisma validate --schema=apps/bff/prisma/schema.prisma</automated>
  </verify>
  <done>Prisma schema defined with core models and PostgreSQL configuration.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Database Health Check in BFF</name>
  <files>apps/bff/src/routes/health.ts, apps/bff/src/app.ts</files>
  <action>
    - Register a `@fastify/prisma` plugin (or custom) for sharing the Prisma client.
    - Implement a `/health` endpoint that calls `prisma.$queryRaw` or similar to verify connectivity.
    - Gracefully handle cases where the DB is unavailable (return 503).
  </action>
  <verify>
    <automated># No automated check without live DB. Build/compile check only.
grep "prisma" apps/bff/src/routes/health.ts</automated>
  </verify>
  <done>BFF health check implemented with database connectivity awareness.</done>
</task>

</tasks>

<verification>
- Prisma schema validates successfully.
- Shared types package is accessible from other workspace apps.
</verification>

<success_criteria>
- The backend has a solid data model and can report its database status.
- Front-end and back-end can share type definitions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-02-SUMMARY.md`
</output>
