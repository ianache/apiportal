---
phase: 01-foundation-identity
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [apps/bff/src/plugins/auth.ts, apps/portal/src/stores/auth.ts, apps/portal/src/main.ts, .env]
autonomous: false
requirements: [IDEN-01, IDEN-02]
user_setup:
  - service: keycloak
    why: "Identity Provider for RBAC"
    env_vars:
      - name: KEYCLOAK_URL
        source: "Self-hosted or Cloud instance"
    dashboard_config:
      - task: "Create Realm 'nexus' and clients"
        location: "Keycloak Admin UI"
      - task: "Configure Roles"
        location: "Roles section (API-Manager, API-Designer, API-Developer)"

must_haves:
  truths:
    - "Keycloak is configured with nexus realm and portal/bff clients"
    - "BFF can validate JWT tokens using openid-client"
    - "Portal can authenticate using Keycloak PKCE"
  artifacts:
    - path: "apps/bff/src/plugins/auth.ts"
      provides: "JWT validation middleware"
    - path: "apps/portal/src/stores/auth.ts"
      provides: "Auth store with keycloak-js"
  key_links:
    - from: "apps/portal"
      to: "Keycloak"
      via: "PKCE flow"
      pattern: 'keycloak\.init'
    - from: "apps/bff"
      to: "Keycloak"
      via: "JWKS / OIDC"
      pattern: 'issuer\.discover'
---

<objective>
Implement the Identity and Access layer using Keycloak v26+.

Purpose: Secure both applications and establish Role-Based Access Control.
Output: Integrated OIDC flow in the BFF and PKCE-based login in the Portal.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-identity/01-RESEARCH.md
@.planning/phases/01-foundation-identity/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Keycloak Infrastructure</what-built>
  <how-to-verify>
    - Access Keycloak Admin Console.
    - Create Realm: `nexus`.
    - Create Client: `nexus-portal` (Public, PKCE, Redirect: `http://localhost:5173/*`).
    - Create Client: `nexus-bff` (Confidential, Service Account enabled).
    - Create Realm Roles: `API-Manager`, `API-Designer`, `API-Developer`.
    - Provide `KEYCLOAK_URL` and `CLIENT_SECRET` (for BFF) in root `.env`.
  </how-to-verify>
  <resume-signal>approved: Keycloak configured and .env populated</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement OIDC Authentication in BFF</name>
  <files>apps/bff/src/plugins/auth.ts, apps/bff/src/app.ts</files>
  <action>
    - Use `openid-client` (v6+) for OIDC discovery and validation.
    - Implement a Fastify plugin that validates the `Authorization: Bearer` token.
    - Populate `request.user` with decoded claims (sub, email, roles).
    - Map Keycloak roles to application roles.
  </action>
  <verify>
    <automated># Build check
npm run build --workspace=apps/bff</automated>
  </verify>
  <done>BFF correctly validates tokens against Keycloak JWKS.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Keycloak with PKCE in Portal</name>
  <files>apps/portal/src/stores/auth.ts, apps/portal/src/main.ts</files>
  <action>
    - Install `keycloak-js` (v26+).
    - Implement `useAuthStore` in Pinia to handle `init`, `login`, and `logout`.
    - Configure `onLoad: 'check-sso'` and `pkceMethod: 'S256'`.
    - Ensure `app.mount()` only occurs after `auth.init()` resolves.
  </action>
  <verify>
    <automated># Build check
npm run build --workspace=apps/portal</automated>
  </verify>
  <done>Portal redirects to Keycloak for login and handles PKCE successfully.</done>
</task>

</tasks>

<verification>
- User can trigger login in the Portal.
- BFF rejects requests with invalid or missing tokens.
</verification>

<success_criteria>
- End-to-end identity flow is established between Portal, BFF, and Keycloak.
- Role information is available in both layers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-03-SUMMARY.md`
</output>
