---
phase: 01-foundation-identity
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: [apps/portal/src/components/layout/Shell.vue, apps/portal/src/views/Dashboard.vue, apps/portal/src/views/Landing.vue, apps/portal/src/router/index.ts]
autonomous: false
requirements: [UI-01]

must_haves:
  truths:
    - "Portal shows a landing page for unauthenticated users"
    - "Authenticated users see a Dashboard shell with Sidebar and Topbar"
    - "User roles are correctly identified and displayed in the UI"
    - "Database connectivity status is visible in the dashboard"
  artifacts:
    - path: "apps/portal/src/components/layout/Shell.vue"
      provides: "Sidebar and Topbar structure"
    - path: "apps/portal/src/views/Dashboard.vue"
      provides: "Main authenticated view"
  key_links:
    - from: "apps/portal/src/views/Dashboard.vue"
      to: "apps/bff/src/routes/health.ts"
      via: "Fetch call"
      pattern: 'fetch.*\/health'
---

<objective>
Build the initial UI shell and verify the complete foundation: Login, Roles, and DB connectivity.

Purpose: Provide a usable entry point for the platform and confirm all systems are operational.
Output: A branded UI shell with role-aware navigation and a diagnostic dashboard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-identity/01-03-SUMMARY.md
@documentations/sketchs/portal.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Layout Shell and Navigation</name>
  <files>apps/portal/src/components/layout/Shell.vue, apps/portal/src/router/index.ts</files>
  <action>
    - Create a `Shell.vue` component implementing the Sidebar/Topbar layout from sketches.
    - Include Material Symbols (notifications, explore, settings, etc.).
    - Configure Vue Router with `Landing` and `Dashboard` routes.
    - Implement a navigation guard that checks `authStore.isAuthenticated`.
  </action>
  <verify>
    <automated># Build check
npm run build --workspace=apps/portal</automated>
  </verify>
  <done>UI Shell implemented with navigation guards and Material Symbols integration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Diagnostic Dashboard</name>
  <files>apps/portal/src/views/Dashboard.vue, apps/portal/src/views/Landing.vue</files>
  <action>
    - Implement `Landing.vue` with a simple "Login to Nexus" call to action.
    - Implement `Dashboard.vue` that displays:
      - Current User Name and Role (from Keycloak).
      - Database Connectivity Status (fetched from BFF `/health`).
      - "Welcome [Role]" message based on detected role.
  </action>
  <verify>
    <automated># Build check
npm run build --workspace=apps/portal</automated>
  </verify>
  <done>Dashboard created with role display and backend health diagnostic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Phase 1 Foundation</what-built>
  <how-to-verify>
    - Start BFF: `npm run dev --workspace=apps/bff`.
    - Start Portal: `npm run dev --workspace=apps/portal`.
    - Visit `http://localhost:5173`.
    - Verify redirection to Keycloak login.
    - Login with a user having `API-Manager` role.
    - Verify name and "API Manager" role appear in Dashboard.
    - Verify "Database: Connected" status appears.
  </how-to-verify>
  <resume-signal>approved: Phase 1 verified and foundation is solid</resume-signal>
</task>

</tasks>

<verification>
- Landing page redirects correctly.
- Dashboard renders role-specific content.
- BFF health check is consumed by the Portal.
</verification>

<success_criteria>
- A developer can log in, see their role, and confirm the backend is connected to the database.
- The UI follows the established design language.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-identity/01-04-SUMMARY.md`
</output>
