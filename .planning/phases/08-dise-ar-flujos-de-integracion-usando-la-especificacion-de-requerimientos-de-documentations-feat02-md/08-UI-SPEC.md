---
phase: 8
slug: 08-dise-ar-flujos-de-integracion-usando-la-especificacion-de-requerimientos-de-documentations-feat02-md
status: approved
shadcn_initialized: false
preset: none
created: 2026-03-30
reviewed_at: 2026-03-30
---

# Phase 8 — UI Design Contract: Integration Flow Designer

> Visual and interaction contract for the Integration Flow Catalog and Visual Designer.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (hand-crafted Tailwind v4 components) |
| Icon library | Material Symbols Outlined (Google Fonts CDN) |
| Font | Inter (Google Fonts CDN) |

**Source:** Detected from `apps/portal/src/style.css` (`@import "tailwindcss"` only, no config)
and `apps/portal/src/components/layout/Shell.vue` (inline `font-family: 'Inter'` + material-symbols).

No shadcn `components.json` found. Project does not use shadcn. Do not initialize — existing
components are hand-crafted. New components for this phase must follow the same inline-style
color convention already established.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, chip internal padding, inline badge gap |
| sm | 8px | Compact element spacing, gap between toolbar buttons |
| md | 16px | Default element spacing, card internal padding, panel padding |
| lg | 24px | Section padding, gap between toolbox items |
| xl | 32px | Layout gaps between major page regions |
| 2xl | 48px | Page-level vertical rhythm in the catalog view |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Canvas drag-drop area: minimum drop target size 44px (touch target accessibility)
- Toolbox filter node items: 44px height minimum (touch-accessible)
- Vue Flow node internal padding: 12px (between xs and md — Vue Flow standard)

**Source:** All spacing tokens derived from existing `Projects.vue` and `Shell.vue` patterns
(p-8 = 32px, p-6 = 24px, gap-3 = 12px, gap-2.5 = 10px round to nearest 8pt).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (regular) | 1.5 |
| Label | 12px (text-xs) | 600 (semibold) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 48px (text-5xl) | 400 (regular) | 1.1 |

**Weights:** Exactly 2 — 400 (regular) for Body and Display roles; 600 (semibold) for Label and
Heading roles. Display prominence is achieved through size (48px) alone, not weight. Weights 700
and 800 are not used in this phase.

**Source:** Detected from `Projects.vue` (text-5xl for page title, text-sm for body labels,
text-xs for section eyebrows). Weight scale reduced to 2 to comply with typography contract rules.

Phase-specific typography notes:
- Node labels inside Vue Flow nodes: 12px, weight 600, line-height 1.2
- Sub-flow tab labels: 14px, weight 600
- Properties panel field labels: 12px, weight 600
- Properties panel field values / inputs: 14px, weight 400

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#faf9fe` | Page background, sidebar background, canvas background |
| Secondary (30%) | `#ffffff` | Cards, modals, toolbox panel, properties panel, active nav items |
| Accent (10%) | `#0058bc` | Primary CTA buttons, active tab indicator, active nav links, connection handles on hover, selected node border |
| Destructive | `#991b1b` | Delete flow action, delete node action only |

Accent reserved for:
- "New Integration Flow" primary CTA button
- "Save Flow" button in the designer toolbar
- Active sub-flow tab indicator (underline/border)
- Selected node border highlight
- Vue Flow edge color on hover/selected state
- Sidebar "Integrations" nav item when active

Supporting palette (from existing codebase, not new introductions):
- `#1a1b1f` — primary text (page titles, node labels)
- `#414755` — secondary text (descriptions, metadata)
- `#717786` — tertiary/muted text (empty state body, disabled labels)
- `#a0a7b5` — placeholder text, section eyebrow labels
- `#e3e2e7` — default borders (cards, panels, dividers)
- `#f4f3f8` — input backgrounds, chip/tag backgrounds, toggle group backgrounds
- `#c7c6d1` — dashed empty state borders
- `#fef2f2` / `#fecaca` / `#fee2e2` — error state surfaces (destructive)

VETRO node type color coding (accent chips only — background fills on node headers):
- Validate: `#0058bc` (blue — accent) with `#e8f0fb` background chip
- Enrich: `#006e28` (green) with `#e6f4ea` background chip
- Transform: `#7c3aed` (purple) with `#f3f0ff` background chip
- Route: `#9e3d00` (amber/brown) with `#fff3e0` background chip
- Operate: `#374151` (dark gray) with `#f1f5f9` background chip

**Source:** Core palette detected from `Shell.vue` and `Projects.vue` inline styles.
VETRO node colors follow the existing API status badge color convention
(green for active, blue for primary, amber/brown for routing).

---

## Layout Contract: Integration Flow Catalog

**Route:** `/integrations` (already in sidebar nav with icon `hub`)

**Layout:** Standard Shell layout (sidebar + header + main content) matching `Projects.vue`.

**Primary visual anchor:** Card grid (`grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`) is the
dominant surface. The "New Integration Flow" CTA button (top-right, accent blue) is the single
highest-contrast interactive element on screen — it must remain visually distinct at all viewport
sizes.

| Zone | Specification |
|------|---------------|
| Page container | `p-8 max-w-7xl mx-auto` |
| Page eyebrow | 12px semibold uppercase tracking-widest, color `#0058bc` — "Integration Flows" |
| Page title | 48px regular, color `#1a1b1f` — "Integrations" |
| Page subtitle | 14px regular, max-w-md, color `#414755` |
| View toggle | Card/Table toggle — identical pattern to `Projects.vue` grid/table toggle |
| Primary CTA | "New Integration Flow" button, accent blue, top-right of header |
| Tag filter bar | Below header, horizontal scrollable row of clickable tag chips |

**Card layout** (when view = card):
- Same `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` grid as APIs catalog
- Each card: `rounded-2xl border p-6`, white background, hover `-translate-y-1`
- Card fields visible: name, description (truncated 2 lines), tags (up to 3 chips), status badge
- Card footer: Edit button (ghost) + Delete button (ghost destructive)

**Table layout** (when view = table):
- Columns: Name, Description, Tags, Status, Actions
- Row hover: `bg-white/70` background
- Actions column: Edit icon button + Delete icon button
- Icon-only buttons in the Actions column MUST include `aria-label` and a visible `title` tooltip:
  - Edit button: `aria-label="Edit [flow name]"` / `title="Edit"`
  - Delete button: `aria-label="Delete [flow name]"` / `title="Delete"`

---

## Layout Contract: Integration Flow Designer

**Route:** `/integrations/:id/designer`

**Layout:** Full-screen override — NO Shell sidebar/header chrome. The designer occupies 100vw × 100vh.

**Primary visual anchor:** The canvas area (`flex-1`, bg `#faf9fe`) is the dominant visual surface.
The canvas must occupy all remaining horizontal space after the 240px toolbox and 280px properties
panel. The Vue Flow instance fills the canvas entirely — no internal scrollbars.

| Zone | Position | Width | Specification |
|------|----------|-------|---------------|
| Top toolbar | top, full width | 100% | 48px height, white bg, border-bottom `#e3e2e7`; contains: back link, flow name (editable inline), sub-flow tabs (Incoming / Response / Exception), Save button |
| Toolbox panel | left | 240px fixed | white bg, border-right `#e3e2e7`; contains: section title "Filters", VETRO node type items (draggable), node count summary |
| Canvas area | center, fills remaining | flex-1 | bg `#faf9fe` (or dot grid from `@vue-flow/background`); Vue Flow instance; empty state overlay when no nodes |
| Properties panel | right | 280px fixed | white bg, border-left `#e3e2e7`; contains: selected node properties form OR empty state "Select a node to configure it" |

**Source:** Mockup at `documentations/images/integration_flow_design.png` shows three-panel layout:
Toolbox (left) + Canvas (center, "Drag-and-Drop Node to create the Flow!") + Properties/Settings Editor (right).
The mockup shows the top bar with "Integration Flow" title and square icon buttons for actions.

---

## Component Inventory

### New Components (Phase 8)

| Component | File | Responsibility |
|-----------|------|----------------|
| `IntegrationFlows.vue` | `views/IntegrationFlows.vue` | Catalog page — card/table list, tag filter, empty state |
| `IntegrationFlowDesigner.vue` | `views/IntegrationFlowDesigner.vue` | Full-screen designer shell — top toolbar + three panels |
| `FlowToolbox.vue` | `components/flow/FlowToolbox.vue` | Left panel — draggable VETRO node items |
| `FlowCanvas.vue` | `components/flow/FlowCanvas.vue` | Center panel — `<VueFlow>` wrapper, drop handler, empty overlay |
| `FlowNodeProperties.vue` | `components/flow/FlowNodeProperties.vue` | Right panel — property form for selected node |
| `FlowSubFlowTabs.vue` | `components/flow/FlowSubFlowTabs.vue` | Top bar tab switcher (Incoming / Response / Exception) |
| `ValidateNode.vue` | `components/flow/nodes/ValidateNode.vue` | Custom Vue Flow node — Validate filter |
| `EnrichNode.vue` | `components/flow/nodes/EnrichNode.vue` | Custom Vue Flow node — Enrich filter |
| `TransformNode.vue` | `components/flow/nodes/TransformNode.vue` | Custom Vue Flow node — Transform filter |
| `RouteNode.vue` | `components/flow/nodes/RouteNode.vue` | Custom Vue Flow node — Route filter |
| `OperateNode.vue` | `components/flow/nodes/OperateNode.vue` | Custom Vue Flow node — Operate filter |
| `FlowStatusBadge.vue` | `components/flow/FlowStatusBadge.vue` | Reusable status chip (DRAFT / ACTIVE / DEPRECATED) |
| `FlowTagChip.vue` | `components/flow/FlowTagChip.vue` | Reusable tag chip for catalog filter and card display |

### Vue Flow Node Anatomy

Each VETRO node component renders:
```
┌─────────────────────────────────┐
│ [color chip] NODE TYPE LABEL    │  ← 12px semibold, chip bg per type
│ ─────────────────────────────── │  ← 1px divider #e3e2e7
│ Node name (editable text)       │  ← 14px regular #1a1b1f
└─────────────────────────────────┘
   ○ [source handle]    ○ [target handle]
```
- Node width: 160px fixed
- Node border: 1px solid `#e3e2e7`, `rounded-xl` (12px)
- Node bg: white (`#ffffff`)
- Node selected border: 2px solid `#0058bc`
- Connection handles: 10px circle, border 2px `#0058bc`, bg white

---

## Interaction Contract

### Catalog Interactions

| Interaction | Trigger | Behavior |
|-------------|---------|----------|
| Open designer | Click card / click Edit button | Navigate to `/integrations/:id/designer` |
| Create flow | Click "New Integration Flow" | Open create modal (name, description, tags) → POST → navigate to designer |
| Delete flow | Click delete button on card/row | Show inline confirmation: "Delete [name]? This cannot be undone." with red "Delete Flow" + "Keep Flow" |
| Filter by tag | Click tag chip in filter bar | Toggle chip active state, filter list client-side |
| Switch view mode | Click Grid / Table toggle | Immediate — no reload |
| Search/filter | Tag chips only (no free-text search in this phase) | Client-side filter by selected tags |

### Designer Interactions

| Interaction | Trigger | Behavior |
|-------------|---------|----------|
| Switch sub-flow | Click Incoming / Response / Exception tab | Swap active Vue Flow instance; preserve node state per sub-flow |
| Drag node from toolbox | `dragstart` on toolbox item | Set `dataTransfer` with node type string |
| Drop on canvas | `drop` event on VueFlow area | `screenToFlowCoordinate()` → `addNodes()` with generated id |
| Select node | Click node | Highlight node border accent blue; populate Properties panel |
| Deselect node | Click canvas background | Clear selection; Properties panel shows "Select a node to configure it" |
| Connect nodes (pipe) | Drag from source handle to target handle | `onConnect` → `addEdges()` — creates directed pipe edge |
| Delete node | Select node → Delete key OR delete button in Properties panel | Show inline confirmation in Properties panel: "Remove this filter from the flow?" with "Remove Filter" + "Keep Filter" actions; on confirm: `removeNodes([nodeId])` — removes node and connected edges |
| Delete edge/pipe | Click edge → Delete key | `removeEdges([edgeId])` |
| Save flow | Click "Save" in toolbar | PUT `/api/flows/:id` with full sub-flow JSON — show success toast / error banner |
| Back to catalog | Click back link in toolbar | `router.push('/integrations')` — warn if unsaved changes |
| Zoom / Pan | Scroll wheel (zoom) / drag canvas bg (pan) | Vue Flow built-in |
| Fit view | Click fit-view in Controls | Vue Flow `fitView()` |

### Empty States

| Location | Trigger | Display |
|----------|---------|---------|
| Canvas (no nodes) | Sub-flow has zero nodes | Centered overlay: icon `hub` (48px, `#a0a7b5`) + "Drag a filter here to start building" (14px `#717786`) |
| Catalog (no flows) | Zero flows in store | Dashed border card identical to `Projects.vue` empty state pattern: icon `schema`, heading "No integration flows yet", body + CTA |
| Properties panel (nothing selected) | No node selected | "Select a node to configure it" — 14px `#a0a7b5`, centered in panel |

---

## Status Badge Contract

| Status | Label | Background | Text Color |
|--------|-------|------------|------------|
| DRAFT | Draft | `#f4f3f8` | `#717786` |
| ACTIVE | Active | `#e6f4ea` | `#006e28` |
| DEPRECATED | Deprecated | `#fff3e0` | `#9e3d00` |

**Source:** Follows existing API status badge pattern from `Projects.vue` (DESIGN=gray, PUBLISHED=green).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (catalog) | "New Integration Flow" |
| Primary CTA (designer save) | "Save Flow" |
| Back link (designer) | "Back to Integrations" |
| Empty state heading (catalog) | "No integration flows yet" |
| Empty state body (catalog) | "Create your first integration flow to start building API orchestrations." |
| Empty state CTA (catalog) | "Create Integration Flow" |
| Empty state (canvas) | "Drag a filter here to start building" |
| Empty state (properties panel) | "Select a node to configure it" |
| Loading state | "Loading flows…" |
| Error state (fetch) | "Failed to load integration flows. Check your connection and try again." |
| Error state (save) | "Failed to save flow. Your changes were not saved — please try again." |
| Delete flow confirmation heading | "Delete integration flow?" |
| Delete flow confirmation body | "This will permanently delete "[flow name]" and all its filter configurations. This cannot be undone." |
| Delete flow confirm button | "Delete Flow" |
| Delete flow cancel button | "Keep Flow" |
| Delete node confirmation | Not modal — inline in Properties panel: "Remove this filter from the flow?" with "Remove Filter" + "Keep Filter" actions |
| Sub-flow tabs | "Incoming" / "Response" / "Exception" |
| Toolbox section heading | "Filters" |
| Toolbox node type labels | "Validate" / "Enrich" / "Transform" / "Route" / "Operate" |
| Unsaved changes warning | "You have unsaved changes. Leave without saving?" with "Leave" + "Stay & Save" |
| Save success toast | "Flow saved successfully." |

---

## RBAC Interaction Gates

| Role | Catalog | Designer |
|------|---------|---------|
| API_MANAGER | Full access — create, edit, delete | Full access — can edit and save |
| API_DESIGNER | Full access — create, edit, delete | Full access — can edit and save |
| API_DEVELOPER | Read-only — view catalog only, no create/edit/delete buttons | Read-only — designer opens in view mode; toolbox hidden; nodes not draggable |

View-only mode indicator: Banner at top of designer canvas — "View only — you do not have permission to edit this flow." (14px, `#717786`, bg `#f4f3f8`).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — project does not use shadcn | not applicable |
| npm (`@vue-flow/core`) | Vue Flow canvas, node/edge model | npm package — not a shadcn registry; standard npm audit applies |
| npm (`@vue-flow/background`) | Dot grid background for canvas | npm package — standard npm audit applies |
| npm (`@vue-flow/controls`) | Zoom/fit-view control panel | npm package — standard npm audit applies |

No third-party shadcn block registries used. Registry vetting gate not applicable.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
